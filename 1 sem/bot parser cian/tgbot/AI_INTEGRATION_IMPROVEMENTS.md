# 📖 Документация улучшений AI Integration

## 🎯 Обзор изменений

Модуль `ai_integration.py` был полностью переработан для максимально точного понимания намерений пользователя при поиске коммерческих помещений. Теперь бот корректно обрабатывает критические параметры, отрицания, приоритеты и срочность запросов.

---

## 🆕 Новые возможности

### 1. **Расширенные параметры поиска**

Функция `extract_search_parameters` теперь возвращает:

#### Базовые параметры (как раньше):
- `city` - город
- `district` - район
- `area_min` / `area_max` - площадь
- `budget` - бюджет
- `floor` - этаж

#### 🔥 НОВЫЕ критичные поля:

- **`excluded_districts`** - список районов для исключения
  ```json
  "excluded_districts": ["Зеленоград", "Митино"]
  ```

- **`excluded_floors`** - этажи-исключения
  ```json
  "excluded_floors": [1, "подвал", -1, 0]
  ```

- **`priority`** - приоритет поиска
  - `"location"` - главное расположение (можно превысить бюджет на 30%)
  - `"price"` - строго в бюджете
  - `"area"` - главное площадь
  - `"balanced"` - сбалансированный (по умолчанию)

- **`urgency`** - срочность (1-10)
  - 9-10: "срочно", "сегодня"
  - 6-8: "в ближайшее время"
  - 3-5: "через месяц"
  - 1-2: "присматриваюсь"

- **`accessibility`** - требования к доступности
  ```json
  "accessibility": "метро Маяковская"
  ```

- **`is_strict`** - жёсткость требований
  - `true` - строгие (исключать несоответствия)
  - `false` - гибкие (допустимы отклонения)

- **`deal_type`** - тип сделки ("rent" / "sale")
- **`renovation_status`** - состояние ("ready" / "rough" / "any")
- **`parking`** - наличие парковки
- **`entrance_type`** - тип входа ("separate" / "common")

---

### 2. **Интеллектуальная обработка формулировок**

#### 🔒 Жёсткие требования (is_strict: true):
```
"только центр" → district: "Центральный", is_strict: true
"строго 100 м²" → area_min: 98, area_max: 102, is_strict: true
```

#### 🚫 Отрицания и исключения:
```
"не рассматриваю окраины" → excluded_districts: [все окраинные]
"без подвала" → excluded_floors: ["подвал", -1, 0]
"кроме первого этажа" → excluded_floors: [1]
```

#### 📏 Числовые параметры с контекстом:
```
"до 100 метров" → area_max: 100, area_min: null
"от 50 квадратов" → area_min: 50, area_max: null
"около 80 кв.м" → area_min: 72, area_max: 88 (±10%)
"примерно 100" → area_min: 85, area_max: 115 (±15%)
"строго 100" → area_min: 98, area_max: 102 (±2%)
```

#### ⚡ Определение приоритетов:
```
"центр", "только центр" → priority: "location"
"главное цена", "не переплатить" → priority: "price"
"нужно много места" → priority: "area"
```

#### ⏰ Срочность:
```
"срочно нужно" → urgency: 9-10
"в ближайшее время" → urgency: 6-8
"через месяц" → urgency: 3-5
```

---

### 3. **Карта районов для городов**

Добавлена константа `DISTRICT_MAPPING` с картой районов для:
- **Москва**: центр, деловой центр, окраины, север, юг, восток, запад
- **Санкт-Петербург**: аналогично
- **Екатеринбург**: аналогично

Это позволяет автоматически расширять запросы типа "центр" на конкретные районы.

---

### 4. **Умное ранжирование в `analyze_listings`**

#### Формула оценки учитывает:

1. **География (до 50 баллов)**:
   - Если `is_strict: true` и район не совпадает → 0 баллов (исключить)
   - Если район в `excluded_districts` → ПОЛНОЕ ИСКЛЮЧЕНИЕ

2. **Бюджет (до 30 баллов)**:
   - Если `priority: "price"` и превышает → 0 баллов
   - Если `priority: "location"` → допустимо +30% для нужного района

3. **Площадь (до 20 баллов)**:
   - Чем ближе к требуемой, тем выше оценка

4. **Доступность (до 25 баллов)**:
   - Повышенный вес, если был дизлайк "далеко от метро"

5. **Срочность (до 20 баллов)**:
   - Если `urgency > 7` → приоритет "готово к заселению"

#### Примеры объяснений (ai_reason):

✅ **Хорошие:**
```
"Идеально: центр + в бюджете, 5 мин до метро"
"⚠️ Превышает бюджет на 30к (15%), но отличное расположение"
"Не в центре, но 3 мин до метро Маяковская + парковка"
```

❌ **Плохие (больше не генерируются):**
```
"Хороший вариант"
"Подходит под критерии"
"Можно рассмотреть"
```

---

### 5. **Валидация критериев (`validate_search_criteria`)**

Новый метод для проверки реалистичности и корректности запросов:

#### Проверяет:

1. **Нереалистичные комбинации**:
   ```
   "центр Москвы" + budget: 50000
   → is_realistic: false
   → warning: "В центре обычно от 150к"
   → suggestion: "Увеличить бюджет или рассмотреть другие районы?"
   ```

2. **Противоречия**:
   ```
   area_min: 150, area_max: 100
   → is_valid: false
   → warning: "Минимум больше максимума"
   ```

3. **Конфликт приоритетов**:
   ```
   priority: "price" + is_strict: true + district: "Центральный"
   → warning: "Конфликт: 'главное цена' + 'только центр'"
   → suggestion: "Что важнее: цена или расположение?"
   ```

4. **Слишком узкие критерии**:
   ```
   area_min: 98, area_max: 102
   → warning: "Очень узкий диапазон (4 м²)"
   ```

---

## 🔧 Системные промпты для GigaChat

### Для `extract_search_parameters`:

```
КРИТИЧНЫЕ ПРАВИЛА ОБРАБОТКИ:

1. ГЕОГРАФИЯ - ЖЁСТКИЕ ТРЕБОВАНИЯ:
   "центр", "только центр" = district: "Центральный", is_strict: true
   
2. ОТРИЦАНИЯ И ИСКЛЮЧЕНИЯ:
   "не", "без", "кроме" → в excluded_*
   
3. ЧИСЛОВЫЕ ПАРАМЕТРЫ:
   "около" → ±10%, "примерно" → ±15%, "строго" → ±2%
   
4. ПРИОРИТЕТЫ:
   "центр" → priority: "location"
   "главное цена" → priority: "price"
   
5. СРОЧНОСТЬ:
   "срочно" → urgency: 9-10
```

### Для `analyze_listings`:

```
КРИТИЧНЫЕ ПРАВИЛА РАНЖИРОВАНИЯ:

1. ГЕОГРАФИЯ (если is_strict: true):
   - Не из района → оценка 0/10
   
2. ПРИОРИТЕТЫ:
   - location: +30% к бюджету ОК
   - price: превышение = 0/10
   
3. ДИЗЛАЙКИ:
   - "далеко от метро" → +50% веса accessibility
   
4. ИСКЛЮЧЕНИЯ:
   - excluded_floors → ПОЛНОСТЬЮ ИСКЛЮЧИТЬ
```

---

## 📊 Логирование для отладки

Все критичные решения логируются в консоль:

```python
🔒 СТРОГИЕ ТРЕБОВАНИЯ: location приоритет
🚫 ИСКЛЮЧЕНЫ РАЙОНЫ: ['Зеленоград', 'Митино']
⚡ СРОЧНЫЙ ЗАПРОС: urgency=9
📍 Расширение 'центр' → Центральный
🏆 #1: ул. Тверская 10 - Идеально: центр + в бюджете
🏆 #2: пр-т Ленина 5 - ⚠️ Превышает бюджет на 30к
```

---

## 🎓 Примеры использования

### Пример 1: Строгий центр

```python
params = await ai_service.extract_search_parameters(
    "Нужно только в центре, не рассматриваю окраины"
)
# Результат:
{
    "district": "Центральный",
    "excluded_districts": ["Зеленоград", "Митино", ...],
    "priority": "location",
    "is_strict": True,
    "urgency": 5
}
```

### Пример 2: Приоритет цена

```python
params = await ai_service.extract_search_parameters(
    "До 150 тысяч, главное не переплатить"
)
# Результат:
{
    "budget": 150000,
    "priority": "price",
    "is_strict": False
}
```

### Пример 3: Валидация

```python
params = await ai_service.extract_search_parameters(
    "Центр Москвы за 50 тысяч"
)
validation = await ai_service.validate_search_criteria(params)
# Результат:
{
    "is_realistic": False,
    "warnings": ["В центре обычно от 150к"],
    "suggestions": ["Увеличить бюджет или рассмотреть другие районы?"]
}
```

---

## 🔄 Интеграция с ботом

### Изменения в `bot.py` (рекомендуется):

```python
# После extract_search_parameters
params = await ai_service.extract_search_parameters(user_message)

# Добавить валидацию
validation = await ai_service.validate_search_criteria(params)

if not validation["is_realistic"]:
    # Показать предупреждения пользователю
    for warning in validation["warnings"]:
        await message.answer(warning)
    
    for suggestion in validation["suggestions"]:
        await message.answer(suggestion)
    
    # Спросить подтверждение
    await message.answer(
        "Продолжить поиск с такими критериями?",
        reply_markup=yes_no_keyboard()
    )
```

---

## 📈 Метрики улучшений

### Было:
- ❌ "центр" мог интерпретироваться как предпочтение
- ❌ Отрицания игнорировались
- ❌ "около 100" = точно 100
- ❌ Приоритеты не учитывались
- ❌ Дизлайки не влияли на поиск

### Стало:
- ✅ "центр" = СТРОГОЕ требование (is_strict)
- ✅ "не рассматриваю" → excluded_districts
- ✅ "около 100" = 90-110 (±10%)
- ✅ priority влияет на допустимость превышения бюджета
- ✅ Дизлайки повышают вес соответствующих критериев

---

## 🚀 Следующие шаги

### Рекомендуется добавить:

1. **Расширить DISTRICT_MAPPING**:
   - Добавить больше городов России
   - Добавить синонимы районов (например, "Сити" = "Пресненский")

2. **Хранение истории дизлайков**:
   ```python
   user_dislikes = {
       user_id: [
           {"listing_id": 123, "reason": "далеко от метро", "timestamp": ...},
           ...
       ]
   }
   ```

3. **Машинное обучение приоритетов**:
   - Анализировать, какие объявления пользователь лайкает
   - Автоматически определять priority на основе истории

4. **Интеграция с картами**:
   - Проверять реальное расстояние до метро через API
   - Показывать объявления на карте с зонами

---

## 📞 Поддержка

При возникновении проблем:

1. **Проверьте логи** в консоли (🔒 🚫 ⚡ 📍 🏆)
2. **Запустите тесты** из `AI_INTEGRATION_TESTS.md`
3. **Проверьте GIGACHAT_CREDENTIALS** в `apis.env`

---

**Версия:** 2.0  
**Дата обновления:** 17.12.2025  
**Автор:** AI Development Team
